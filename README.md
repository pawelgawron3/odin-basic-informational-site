# ☕ Morning Haze

Morning Haze is a simple, informational coffee shop website built with Node.js and the native `http` module. The project uses server-side rendering, custom routing, form handling, and static file serving without frameworks such as Express or EJS.
